# Copy this file to .ingest.env and fill in your ingest server settings
# This lets ./install.sh work with zero flags (ingest mode)

# Base URL of your ingest service (no trailing /ingest)
ZECX_INGEST_URL=http://127.0.0.1:5000

# Optional bearer token (also set INGEST_BEARER_TOKEN on the server)
ZECX_INGEST_TOKEN=
